{
	"name": "scheduler",
	"version": "0.2.1",
	"description": "Schedule Obsidian commands to run at specific times or intervals",
	"main": "main.js",
	"scripts": {
		"dev": "bun run esbuild.config.mjs",
		"lint": "bun run eslint src/",
		"lint:fix": "bun run eslint --fix src/",
		"format": "biome format --write src/",
		"build": "bun run esbuild.config.mjs production",
		"stage:local:prep": "dotenv -- mkdir -p \"$DEPLOY_DIR\"",
		"stage:local": "dotenv -- cp main.js manifest.json styles.css \"$DEPLOY_DIR\"",
		"deploy:local": "dotenv -- sh -c 'bun run --sequential stage:local:prep stage:local'",
		"deploy": "dotenv -- bun run --sequential lint:fix build deploy:local",
		"release": "rm -f scheduler.zip && bun run build && mkdir -p scheduler && cp main.js manifest.json styles.css scheduler && zip scheduler.zip scheduler"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"scheduler",
		"cron",
		"automation"
	],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@biomejs/biome": "^2.4.6",
		"@types/node": "^25.3.0",
		"esbuild": "^0.27.3",
		"globals": "^17.4.0",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	},
	"dependencies": {
		"cron-parser": "^5.5.0",
		"dotenv-cli": "^11.0.0",
		"eslint": "^10.0.3",
		"eslint-plugin-obsidianmd": "^0.1.9"
	}
}
